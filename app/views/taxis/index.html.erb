<p id="notice"><%= notice %></p>

<h1>Sitios de Taxi</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Titular</th>
      <th>Latitud</th>
      <th>Longitud</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @taxis.each do |taxi| %>
      <tr>
        <td><%= taxi.nombreBase %></td>
        <td><%= taxi.nombreTitular %></td>
        <td><%= taxi.latitud %></td>
        <td><%= taxi.longitud %></td>
        <td><%= link_to 'Show', taxi %></td>
        <td><%= link_to 'Edit', edit_taxi_path(taxi) %></td>
        <td><%= link_to 'Destroy', taxi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Taxi', new_taxi_path %>
